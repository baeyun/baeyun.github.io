{
  "name": "baeyun.github.io",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:static": "next build && next export",
    "deploy": "yarn build:static && git add out/ && git commit -m \"Deploy site to gh-pages\" && git subtree push --prefix out origin gh-pages",
    "start": "next start"
  },
  "dependencies": {
    "@zeit-ui/react": "^1.8.0",
    "@zeit-ui/react-icons": "^1.3.0",
    "next": "9.5.2",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
